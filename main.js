/* Math.pow

El método Math.pow() devuelve el valor de x elevado a y (xy).
Basicamente eleva el primer valor al numero que se pone luego
*/
let x = Math.pow(4, 3); // muestra 64, es como poner 4 elevado a la 3, 4*4 = 16; 16*4 = 64
let a = Math.pow(0, 1); // muestra 0 
let b = Math.pow(1, 1); // muestra 1 
let c = Math.pow(1, 10); // muestra 1
let d = Math.pow(3, 3); // muestra 27 
let e = Math.pow(-3, 3); // muestra -27
let f = Math.pow(2, 4); // muestra 16